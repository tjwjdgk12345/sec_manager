# CS40008 웹취약점 탐지 AI Agent — 프로젝트 계획

> 이 문서는 Claude Code 등 코드 어시스턴트가 프로젝트 맥락을 파악하기 위한 참고 파일입니다.
> 진행 상황에 따라 체크박스와 "현재 상태" 섹션을 계속 업데이트해서 사용하세요.

## 1. 프로젝트 개요

- **과목**: CS40008 전산학 프로젝트
- **목표**: 웹 사이트를 대상으로 AI Agent가 화면과 요청 경로를 탐색해 XSS, SQLi, IDOR 후보를 찾고, 제한된 실습 환경에서 PoC로 재현·검증한 뒤 결과를 보고서로 정리하는 자동화 파이프라인 구현
- **처리 흐름**: 수집 → 분석 → PoC 코드 구현 → 검증 → 결과 정리
- **테스트 대상**: OWASP Juice Shop (수업 측에서 웹 서버 오픈 후 제공 예정, 아직 미제공)
- **다루는 취약점**: XSS, SQLi, IDOR — 각 1개 시나리오씩 선정
- **제약 조건**:
  - 브라우저 자동화·HTTP 요청 재현은 허용된 테스트 사이트에만 수행
  - LLM은 일반 PC에서 실행 가능한 경량 모델 사용
- **개발 환경**: Windows 네이티브 (WSL 아님 — GNN 에이전트 프로젝트와 구분)
- **기술 스택**: Python, Selenium + Chrome, Chrome DevTools Protocol(CDP) 로그 수집
- **저장소**: `KAIST-CS40008/2026f-11-agent-web-vuln-analysis`
  - 주간 보고서는 `reports/week-XX-report.md`로 PR 제출
  - PR 생성 시 TA(`slatfatf42`)가 자동으로 리뷰어로 지정되도록 GitHub Actions 워크플로 설정 완료
- **멘토링**: 매주 금요일 진행 상황 논의, 막히는 부분/범위 변경은 즉시 공유, 동작하는 코드는 시연 후 다음 단계 여부 결정

## 2. 현재 상태 (진행 상황 요약)

- [x] 프로젝트 범위 및 12주 계획 파악
- [x] 개발 환경을 Windows 네이티브로 결정
- [x] GitHub Actions로 PR 자동 리뷰어 지정 워크플로 구현 및 머지
- [ ] Juice Shop 테스트 서버 수령 대기 중 (수업 측 제공 예정)
- [x] Python/Selenium/Chrome 로컬 개발 환경 설치 (.venv, selenium 4.48, requests)
- [x] 프로젝트 뼈대 구성 (src/llm, src/agent, src/browser, examples, docs)
- [ ] Juice Shop 없이도 개발 가능한 부분(PageExplorer, NetworkCollector, Agent 도구 인터페이스)을 범용 웹사이트 대상으로 먼저 구현 진행 중
- [ ] Ollama 로컬 모델 설치 및 실 루프 검증 (현재 MockClient로 루프 구조 검증 완료)

## 3. 주차별 할 일

### 1주차 — Agent 실행 방법 및 기초 예제

- [x] XSS, SQLi, IDOR 각각의 발생 시나리오와 필요한 사전 조건, 성공 판정 근거 정리 → `docs/vuln-scenarios.md`
- [x] Python 환경에서 AI Agent 기초 예제 구현 (LLM이 도구 하나를 호출하고 결과를 받는 최소 루프) → `src/agent/loop.py`, `examples/minimal_agent_loop.py`
- [x] Selenium으로 접속, 로그인, 쿠키 유지 예제 구현 → `src/browser/session.py`, `examples/selenium_login_demo.py`

### 2-3주차 — 페이지 및 네트워크 정보 수집

- [ ] Agent가 목표에 맞는 탐색 도구를 고를 수 있도록 도구 설명·반환 형식 정리 (도구 스키마 설계)
- [ ] `PageExplorer` 구현: 동일 호스트 내 링크/버튼/폼/입력 필드 탐색, 중복 방문 방지, 탐색 깊이 제어
- [ ] `NetworkCollector` 구현: CDP 로그에서 요청·응답 메타데이터, API 경로, 파라미터, JS 리소스 추출
- [ ] 페이지 구조 + 네트워크 경로를 연결해 탐지기가 쓸 입력 정보/웹 환경 프로파일 구성
- [ ] (선행 작업) Juice Shop 없이 범용 테스트 사이트로 위 두 도구 먼저 검증

### 4주차 — Agent 판단 조건 및 오케스트레이션 루프

- [ ] 수집 결과를 읽고 다음 탐지 도구를 선택하는 판단 규칙, 상태 관리, 종료 조건 구현
- [ ] 도구 호출 횟수·실행 시간 상한 설정 (무한 루프 방지)
- [ ] 각 도구의 입출력 형식 통일 (탐지 단계와 검증 단계가 같은 구조 공유)

### 5주차 — XSS 후보 탐지

- [ ] `XSSDetector` 구현: 입력값 반영 위치, DOM 변화, 스크립트 실행 여부 확인
- [ ] 반사형 / 저장형 / DOM 기반 유형 구분 및 유형별 판단 기준 정리
- [ ] 후보별 재현 절차 + 증거(요청/응답, 화면 변화) 기록

### 6주차 — SQLi 후보 탐지

- [ ] `SQLiDetector` 구현: 원본 요청 vs 변형 요청의 상태 코드, 오류 메시지, 응답 본문, 응답 시간 비교
- [ ] 오류 기반 / 불리언 기반 / 시간 기반 신호 구분 및 신뢰도 스코어링
- [ ] 요청 변형 규칙과 재시도 한도 설정 (테스트 사이트 부하 제한)

### 7주차 — IDOR 후보 탐지

- [ ] `IDORDetector` 구현: 제공된 테스트 계정 간 객체 접근 결과 비교
- [ ] 식별자 값 변경, 권한 이동 패턴 점검, 접근 허용 시 근거 기록
- [ ] 인증/권한 정보가 필요한 경로 정리 (오탐과 정상 동작 구분 기준 마련)

### 8주차 — 탐지 결과 통합 및 우선순위화

- [ ] 세 탐지기 결과·증거를 하나의 형식으로 통합, 중복·오탐 정리
- [ ] 재현 가능성과 영향도 기준으로 PoC 대상 후보 우선순위화
- [ ] 후보별 필요 입력값, 계정, 사전 조건 정리해 검증 단계로 전달

### 9-10주차 — 검증 및 리포트 자동 생성

- [ ] 도구 실행 결과를 Agent에 반영해 다음 도구 선택/종료하는 반복 실행 흐름 완성
- [ ] `CRetriever`, `CGenerator`, `CRunner` 구현 — 최소 재현 코드 생성, 실행 시간·요청 횟수 제한
- [ ] `Verifier` 구현 — 공격 성공 여부 판정, 실패/증거 부족 시 코드 수정 후 재실행
- [ ] `ReportGenerator` 구현 — 공격 결과 + 탐지 근거를 포함한 보고서 자동 생성

### 11-12주차 — 결과 정리와 발표 준비

- [ ] 전체 파이프라인 통합 테스트
- [ ] 발표 자료 준비

## 4. 결정 사항 로그 (변경 시 이 섹션에 추가)

- Windows 네이티브 환경에서 개발 (WSL 미사용)
- Juice Shop 미제공 기간 동안 PageExplorer/NetworkCollector 등 대상-불특정 구성요소를 범용 사이트로 선행 개발
- Selenium 4 내장 Selenium Manager 사용 (ChromeDriver 수동 관리 불필요)
